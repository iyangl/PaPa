<resources>

    <!-- Base application theme. -->
    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 防止欢迎页白屏或者黑屏，设置图片 -->
    <style name="SplashTheme" parent="Base.AppTheme">
        <item name="android:windowBackground">@drawable/app_splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowIsTranslucent">false</item>-->
        <!--<item name="android:windowDisablePreview">true</item>-->
    </style>

    <style name="AppTheme" parent="Base.AppTheme"/>

    <style name="Bottom_bar_text">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textSize">@dimen/bottom_text_size</item>
        <item name="android:layout_marginTop">@dimen/bottom_text_margin</item>
    </style>

    <style name="Bottom_bar_icon">
        <item name="android:layout_width">@dimen/bottom_icon_size</item>
        <item name="android:layout_height">@dimen/bottom_icon_size</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="Theme.ToolBar.PaPa" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.PaPa</item>
    </style>

    <style name="ActionButton.Overflow.PaPa" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/main_menu_more_overflow_mtrl_alpha</item>
    </style>

</resources>
